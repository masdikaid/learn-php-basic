<?php

$a = true;
$b = false;

// and
var_dump($a && $b);
var_dump($a and $b);

// or
var_dump($a || $b);
var_dump($a or $b);

// not 
var_dump(!$b);

// xor 
var_dump($a xor $b); // true just if one of them is true, not both of them



